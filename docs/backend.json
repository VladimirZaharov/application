{
  "entities": {
    "Project": {
      "title": "Project",
      "type": "object",
      "description": "Represents a proposal project that can be saved, edited, and shared.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Project entity."
        },
        "name": {
          "type": "string",
          "description": "The unique name of the project."
        },
        "proposal": {
          "type": "string",
          "description": "JSON data for the proposal state."
        },
        "branding": {
          "type": "string",
          "description": "JSON data for the branding state."
        },
        "selectedProblems": {
          "type": "string",
          "description": "JSON data for the selected problems."
        },
        "auditGoalText": {
          "type": "string",
          "description": "Text for the audit goal section."
        },
        "trafficAnalysisText": {
          "type": "string",
          "description": "Text for the traffic analysis section."
        },
        "growthPointsText": {
          "type": "string",
          "description": "Text for the growth points section."
        },
        "lastModified": {
          "type": "string",
          "description": "Timestamp of the last modification.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "proposal",
        "branding",
        "selectedProblems",
        "auditGoalText",
        "trafficAnalysisText",
        "growthPointsText",
        "lastModified"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "rules": "rules_version = '2';\n\nservice cloud.firestore {\n  match /databases/{database}/documents {\n\n    // Match any document in the 'users' collection\n    match /users/{userId} {\n      allow read, write: if request.auth != null && request.auth.uid == userId;\n\n      // Match any document in the 'projects' subcollection\n      match /projects/{projectId} {\n        allow read, write: if request.auth != null && request.auth.uid == userId;\n      }\n    }\n  }\n}",
    "collections": [
      {
        "path": "/users/{userId}/projects",
        "schema": {
          "$ref": "#/entities/Project"
        },
        "description": "Stores project data owned by a specific user."
      }
    ]
  }
}
